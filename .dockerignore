.git
# Dev-only, gitignored, nested checkout of github.com/GNU-Ymir/bootstrap - the release build
# fetches its own copy fresh at the exact release tag instead (see Dockerfile).
bootstrap/
binding/*.o
*~
*#
.#*
